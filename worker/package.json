{
  "name": "osrs-spawn-map-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_SEND_METRICS=false wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint:check": "oxlint -c ../.oxlintrc.json .",
    "lint:write": "oxlint -c ../.oxlintrc.json --fix .",
    "format:check": "oxfmt --check .",
    "format:write": "oxfmt ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "oxfmt": "^0.51.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
